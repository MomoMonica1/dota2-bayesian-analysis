# Local analysis outputs

The runner writes its prepared data, audit counts, fitted models, posterior summaries, sampler diagnostics, trace plots, configuration, and session information here. Generated files are ignored by Git.

No research results are included because the source CSVs were not supplied. Historical slide estimates are documented separately in `docs/methodology.md`.
